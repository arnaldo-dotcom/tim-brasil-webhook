{
  "name": "tim-brasil-webhook",
  "version": "1.0.0",
  "description": "Mock webhook para demo do agente Bia — TIM Brasil (Moveo.AI)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  }
}
